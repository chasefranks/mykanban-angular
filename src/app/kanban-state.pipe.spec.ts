import { KanbanStatePipe } from './kanban-state.pipe';

describe('KanbanStatePipe', () => {
  it('create an instance', () => {
    const pipe = new KanbanStatePipe();
    expect(pipe).toBeTruthy();
  });
});
